@page
@model IndexModel
@{
    ViewData["Title"] = "Hồ sơ cá nhân";
    ViewData["ActivePage"] = ManageNavPages.Index;
    Layout = "/Areas/Identity/Pages/Account/Manage/_Layout.cshtml";
}

@* 
    Đây là cấu trúc chuẩn được lấy từ mẫu "Đổi mật khẩu" của bạn.
    - Card: tạo khung trắng, có đổ bóng và không có viền.
    - Card Header: tạo thanh tiêu đề màu xanh, chứa icon và tên trang.
    - Card Body: chứa form, có khoảng đệm p-4 (padding 4) rộng rãi.
*@

<div class="card shadow-sm border-0">
    <div class="card-header bg-primary text-white">
        <h4 class="mb-0">
            <i class="fas fa-id-card me-2"></i>@ViewData["Title"]
        </h4>
    </div>
    <div class="card-body p-4">
        <form id="profile-form" method="post">
            @* Luôn đặt StatusMessage và ValidationSummary ở đầu form *@
            <partial name="_StatusMessage" for="StatusMessage" />
            <div asp-validation-summary="ModelOnly" class="text-danger mb-3" role="alert"></div>

            @* Trường Tên đăng nhập (Email) - Bị vô hiệu hóa, chỉ để hiển thị *@
            <div class="form-floating mb-3">
                <input asp-for="Username" class="form-control" autocomplete="username" disabled placeholder="Tên đăng nhập" />
                <label asp-for="Username" class="form-label"></label>
            </div>

            @* Trường Họ và Tên *@
            <div class="form-floating mb-3">
                <input asp-for="Input.FullName" class="form-control" placeholder="Vui lòng nhập họ và tên của bạn." />
                <label asp-for="Input.FullName" class="form-label"></label>
                <span asp-validation-for="Input.FullName" class="text-danger"></span>
            </div>

            @* Trường Ngày sinh *@
            <div class="form-floating mb-3">
                <input asp-for="Input.DateOfBirth" class="form-control" type="text" onfocus="(this.type='date')" onblur="(this.type='text')" placeholder="Vui lòng nhập ngày sinh của bạn." />
                <label asp-for="Input.DateOfBirth" class="form-label"></label>
                <span asp-validation-for="Input.DateOfBirth" class="text-danger"></span>
            </div>

            @* Trường Số điện thoại *@
            <div class="form-floating mb-3">
                <input asp-for="Input.PhoneNumber" class="form-control" autocomplete="tel" placeholder="Vui lòng nhập số điện thoại của bạn." />
                <label asp-for="Input.PhoneNumber" class="form-label"></label>
                <span asp-validation-for="Input.PhoneNumber" class="text-danger"></span>
            </div>

            @* Nút bấm Lưu thay đổi *@
            <button id="update-profile-button" type="submit" class="w-100 btn btn-lg btn-primary mt-3">Lưu thay đổi</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}