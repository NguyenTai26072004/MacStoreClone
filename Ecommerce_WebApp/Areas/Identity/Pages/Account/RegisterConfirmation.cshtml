@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Xác nhận Đăng ký";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6 col-xl-5">

            <div class="card shadow-lg" style="border-radius: 1rem;">
                <div class="card-body p-4 p-sm-5 text-center">

                    <div class="mb-4">
                        <i class="fa-solid fa-paper-plane fa-3x text-primary"></i>
                    </div>

                    <h3 class="fw-bold mb-3">@ViewData["Title"]</h3>

                    <p class="text-muted">
                        Đăng ký gần như hoàn tất! Một email xác thực đã được gửi đến địa chỉ <strong>@Model.Email</strong>.
                    </p>
                    <p class="text-muted">
                        Vui lòng kiểm tra hộp thư của bạn và nhấp vào liên kết bên trong để kích hoạt tài khoản.
                    </p>

                    <hr class="my-4">

                    <p class="small">
                        Không nhận được email?
                        <a asp-page="./ResendEmailConfirmation" class="text-decoration-none">Gửi lại</a>.
                    </p>

                </div>
            </div>
        </div>
    </div>
</div>