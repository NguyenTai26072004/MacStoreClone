@page
@model ResendEmailConfirmationModel
@{
    ViewData["Title"] = "Gửi lại email xác thực";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6 col-xl-5">

            <div class="card shadow-lg" style="border-radius: 1rem;">
                <div class="card-body p-4 p-sm-5">

                    <div class="text-center mb-4">
                        <img src="~/img/logo.png" alt="Logo" height="40" />
                    </div>

                    <h3 class="fw-bold mb-3 text-center">@ViewData["Title"]</h3>
                    <p class="text-muted text-center small mb-4">
                        Nhập địa chỉ email bạn đã dùng để đăng ký và chúng tôi sẽ gửi lại liên kết xác thực cho bạn.
                    </p>

                    <form method="post">
                        @* Hiển thị thông báo thành công hoặc lỗi ở đây *@
                        <div asp-validation-summary="ModelOnly" class="text-success" role="alert"></div>

                        <div class="form-floating mb-3">
                            <input asp-for="Input.Email" class="form-control" aria-required="true" placeholder="name@example.com" />
                            <label asp-for="Input.Email" class="form-label"></label>
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                        </div>

                        <div class="d-grid mt-4">
                            <button type="submit" class="btn btn-primary btn-lg fw-bold">Gửi lại</button>
                        </div>
                    </form>

                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}